# Logging Guidelines

> Not applicable — this project is a JSON + git marketplace with no runtime backend code.

For plugin hooks that include scripts, see [Hooks Guidelines](../frontend/hook-guidelines.md) for exit code conventions and stderr/stdout behavior.
